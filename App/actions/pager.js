import ＊ as types from './actionTypes';

var App = require('../serive/core.app');



export function initData() {
    
}

export function week_star_rank() {
    return {
        type: types.WEEK_STAR_RANK
    }
}

export function week_score_rank() {
    return {
        type: types.WEEK_SCORE_RANK
    }
}